/**
 * GRACE-X Landing Page Config
 * Toggle: set enabled to false to disable landing and skip to main app
 * Key: change LANDING_KEY to set the unlock key
 */
window.LANDING_CONFIG = {
  enabled: true,
  key: "gracex2026"
};
